#include <stdio.h>

int main(int argc, char *argv[]){
    //Main returns an integer and takes two arguments, an int "argc"
    //or "argument count", which counts the number of command line
    //arguments, and an array of pointers called argv that points to
    //strings which are the actual arguments typed in when the program
    //is compiled and ran from the command line. 
    printf("Hello, world!");
    return 0;
}